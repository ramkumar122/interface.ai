"""CoreDesk -- the target application package.

This package contains only the mock credit-union web app: routes, tenant
config, session helpers, templates, and styling. Per the repository structure
rules, it must never import from /agent, /replay, or /control -- the target app
does not know it is being automated.
"""
