"""The operator console: a separate process that renders what the system
already produces.

Imports ``artifacts/``, ``replay/``, ``control/`` and ``surface/``.  Never
``coredesk/`` — the target app does not know this exists.
"""
